// 必要なHTML要素の取得
let wrap = document.getElementById('typing-container-inner');
let start = document.getElementById('start');

let textLists = [
  'テストようもんだい',
  'テストようもんだい1',
  'テストようもんだい3',
]; // 複数のテキストを格納する配列


let createText = () => {

}; // ランダムなテキストを画面に表示する

let keyDown = e => {}; // キーイベント＆入力判定処理

let rankCheck = rank => {}; // ランク判定とメッセージ生成処理

let gameOver = id => {}; // ゲームの終了処理

let timer = () => {}; // タイマー処理


start.addEventListener('click', () => {}); // ゲームスタート時の処理